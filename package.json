{
  "name": "qa-test-hub",
  "version": "1.0.0",
  "description": "Aggregated QA test dashboard across all projects",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx serve dist"
  },
  "engines": {
    "node": ">=20"
  }
}
